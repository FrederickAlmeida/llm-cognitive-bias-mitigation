debiaser:
  llm_provider: "groq"
  llm_model: "openai/gpt-oss-20b"

model:                             # model being evaluated for cognitive bias
  llm_provider: "groq"
  llm_model: "openai/gpt-oss-20b"

dataset:
  name: "jecht/cognitive_bias"
  biases:
    - framing
    - group_attribution
    - status_quo
    - primacy
    - anchoring
  limit: null

output:
  results_dir: "results/"

prompts_dir: "prompts/"
